FROM python:3.12-slim

WORKDIR /app

# ffmpeg : habillage DÉTERMINISTE (S188) — cartes de titre (drawtext) + sous-titres incrustés
# (filtre subtitles, libass). Aucun réseau/clé requis, contrairement aux fournisseurs de
# génération vidéo (ceux-là restent hébergés, cf. fournisseurs.py).
RUN apt-get update && apt-get install -y --no-install-recommends ffmpeg \
    && rm -rf /var/lib/apt/lists/*

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "5970"]
