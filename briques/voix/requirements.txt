# Brique voix (TTS) — dépendances minces et épinglées.
# Le moteur SOUVERAIN local = Piper, fourni par le BINAIRE précompilé installé dans le
# Dockerfile (auto-contenu : espeak-ng-data + libs) — pas un paquet pip (piper-phonemize
# n'a pas de wheel hors x86). Les fournisseurs hébergés (OpenAI/ElevenLabs/Gateway) ne
# demandent que httpx. Piper a besoin d'un modèle de voix (.onnx) désigné par PIPER_VOICE.
fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
# Chat vocal TEMPS RÉEL (realtime.py) : relais WebSocket vers OpenAI/Gemini/Grok.
# Client WS amont + support WebSocket côté serveur FastAPI/uvicorn.
websockets==14.1
wsproto==1.2.0
# Bibliothèque de voix CLONÉES (clones.py) : upload de l'échantillon de référence (WAV) via
# UploadFile/Form (multipart). Léger, sans dépendance lourde (le clonage = Coqui XTTS opt-in).
python-multipart==0.0.20
