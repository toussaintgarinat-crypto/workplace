[pytest]
# S206 — périmètre du filet rapide de cette brique.
#
# `test_detecteur.py` est exclu : il charge les modèles ONNX d'openwakeword (téléchargés par
# le Dockerfile, absents d'une image nue) et compare des ÉCHANTILLONS AUDIO qui ne sont pas
# versionnés. Il ne peut donc rien dire sur un poste ou une CI vierge — et son échec masquait
# les six autres fichiers, qui eux testent du code parfaitement vérifiable (authentification,
# catalogue, commandes, isolation par personne, entraînement, paiement).
#
# Pour le lancer réellement : depuis l'image de la brique, où les modèles sont présents.
testpaths =
    test_auth.py
    test_catalogue.py
    test_commandes.py
    test_entrainement.py
    test_isolation.py
    test_paiement.py
