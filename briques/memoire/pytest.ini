[pytest]
# S206 — périmètre du filet rapide de cette brique.
#
# `testpaths` exclut délibérément `memory/backend/tests/` : ces tests viennent du sous-projet
# Memory (tiers, vendored) et sont des tests d'INTÉGRATION qui supposent un schéma déjà
# migré — ils échouent en « relation users does not exist » même avec un Postgres+pgvector
# fourni, parce qu'aucune migration n'est jouée avant. Les faire tourner demande de câbler
# Alembic dans le harnais : c'est un chantier à part, pas un préalable au filet.
#
# Ce qui est couvert ici, ce sont les tests écrits POUR Workplace (isolation par personne,
# proxy SPA, espaces, configuration) — soit exactement le code que nos sprints touchent.
# Dette assumée et tracée : les tests d'intégration du backend Memory restent hors filet.
testpaths =
    test_memoire.py
    test_episteme.py
    test_isolation_personne.py
    test_espace_custom_personne.py
    test_spa_personne.py
    test_config_settings.py
