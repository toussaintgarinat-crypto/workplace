# Brique gateway-sync — un endpoint et deux appels HTTP. Dépendances minces et épinglées,
# alignées sur constraints-workplace.txt (cf. make deps-audit).
fastapi==0.115.6
uvicorn[standard]==0.34.0
httpx==0.28.1
